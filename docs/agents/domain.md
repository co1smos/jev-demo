# Domain documentation

This is a single-context repository.

Before implementation, read `CONTEXT.md` when present and relevant ADRs under `docs/adr/`. Use the project's defined vocabulary in tests, issue titles, and interfaces. Surface conflicts with an ADR rather than silently overriding it.

Create domain documentation lazily when resolved vocabulary or durable decisions justify it; absence is not an error.
