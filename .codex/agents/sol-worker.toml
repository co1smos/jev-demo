name = "sol_worker"
description = "General software engineering worker for tasks requiring non-trivial reasoning."

model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

developer_instructions = """
Implement the requested change end-to-end.
Prefer the smallest correct solution.
Run relevant tests and verify behavior.
Do not broaden scope unnecessarily.
"""
